server:
  port: 8080
spring:
  application:
    name: profiles
  datasource:
    password: admin
    url: jdbc:postgresql://localhost:5432/profiles
    username: fatih
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true